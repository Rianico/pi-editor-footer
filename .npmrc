engine-strict=false
audit=false
fund=false
progress=false
# For pnpm projects, use pnpm-workspace.yaml allowBuilds; for npm, allow scripts via trusted
# Speed up pi -e by ignoring unnecessary install scripts for extension dev
